using UnityEngine;

public class Spline : MonoBehaviour {
    //rotation minimizing frames/parallel transport

    //hermite, b-splines and catmull-rom, with plans of supporting beziers too

    //rotate the points with interpolation done with a quaternionic catmull-rom
}